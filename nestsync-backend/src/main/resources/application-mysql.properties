spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/nestsync}
spring.datasource.username=${DB_USERNAME:nestsync}
spring.datasource.password=${DB_PASSWORD}
# Local/container MVP only. Introduce migrations before a production rollout.
spring.jpa.hibernate.ddl-auto=update
nestsync.jwt.allow-ephemeral=false
